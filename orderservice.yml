server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dataloader
    username: root
    password: Gnanesh@17091

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true
