server:
  port: 9000

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:

        - id: order-service
          uri: lb://ORDERSERVICE
          predicates:
            - Path=/orders/**

        - id: payment-service
          uri: lb://PAYMENTSERVICE
          predicates:
            - Path=/payment/**
